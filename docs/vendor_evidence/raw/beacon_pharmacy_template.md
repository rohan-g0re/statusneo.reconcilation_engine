---
source_url: https://support.beaconchannelmanagement.com/en/articles/9723402-data-template-pharmacy-claims-data
article_id: 9723402
article_title: "Data Template: Pharmacy Claims Data"
article_subtitle: "Streamline data processes with Beacon's data template"
article_date_on_page: "December 10, 2025"
sitemap_lastmod: "2025-12-10T18:47:15Z"
vendor: Beacon (Beacon Channel Management, operated by Second Sight Solutions)
retrieval_method: "r.jina.ai text-extraction proxy — the direct fetch returns HTTP 403"
retrieved_on: 2026-09-17
evidence_tier: RETRIEVED_VIA_PROXY
evidence_tier_rationale: >
  The text below is genuine Beacon page content, but it did not arrive from a
  clean first-hand fetch. support.beaconchannelmanagement.com is Intercom-hosted
  and returns HTTP 403 to direct automated requests (confirmed this session:
  a bare curl of this exact URL returned 403). The content was obtained by
  routing the request through the third-party text-extraction proxy r.jina.ai,
  which fetched the page and returned its rendered text. That places one party
  we do not control between us and the vendor, and it flattens the page's HTML
  table into run-together text. Both are reasons to rank this a notch below a
  first-hand fetch. It is strong enough to cite for field names and required
  markers; it is NOT strong enough to certify the byte-exact header row of the
  downloadable template file, which we never obtained (see "What this page did
  not tell us").
field_count: 11
fields_marked_required: 11
fields_not_marked_required: 0
---

# Beacon — Data Template: Pharmacy Claims Data

Retrieved 2026-09-17 via `https://r.jina.ai/https://support.beaconchannelmanagement.com/en/articles/9723402-data-template-pharmacy-claims-data` (HTTP 200).
Direct fetch of the same URL without the proxy: **HTTP 403**.

---

## 1. Verbatim excerpt

Everything between the fences is the proxy's returned text, unaltered — including
the run-together bold markers where Beacon's HTML table was flattened, the
curly quotation marks, and the stray `_._` italic artifact on the first row.

```
Title: Data Template: Pharmacy Claims Data

URL Source: https://support.beaconchannelmanagement.com/en/articles/9723402-data-template-pharmacy-claims-data

Markdown Content:
1.   [All Collections](https://support.beaconchannelmanagement.com/en/)
2.   [Using Beacon Rebate Model](https://support.beaconchannelmanagement.com/en/collections/11972931-using-beacon-rebate-model)
3.   [Data Submissions](https://support.beaconchannelmanagement.com/en/collections/10023718-data-submissions)
4.   [Data Templates](https://support.beaconchannelmanagement.com/en/collections/9974649-data-templates)
5.   Data Template: Pharmacy Claims Data

Streamline data processes with Beacon's data template

December 10, 2025

## Download the pharmacy claims data template here:

## View the field list for pharmacy claims below

**Field****Data Type****Description**
**340B ID***Alpha/Numeric The unique identification number provided by HRSA to the 340B covered entity _._
**Date Prescribed***Standard date formats Date the prescriber wrote the prescription.
**Date of Service***Standard date formats Date on which the pharmacy filled the prescription.
**Rx Number***Numeric The native (unmodified) prescription number for the prescription as generated by the pharmacy.
**Fill Number***Numeric - 0-99 Indicates the number of times a prescription has been filled.
**NDC-11***Numeric - 11 digits The 11-digit National Drug Code which indicates the manufacturer, product, and the commercial package size.
**Quantity Dispensed***Numeric The number of units dispensed to the patient.
**Prescriber ID***Numeric - 10 digits National provider identifier (NPI) of the physician that wrote the prescription.
**Service Provider ID***Numeric - 10 digits NPI of the pharmacy that filled the prescription.
**Rx Bin***Numeric - 6 digits Prescription Drug Bank Identification Number. Enables pharmacies to electronically transmit data to the appropriate PBM for processing and reimbursement. Include BIN for the primary payer on the claim. If patient is uninsured or a cash payer, mark “999999” in this field.
**Rx PCN***Alpha/Numeric Processor Control Number. Identifier used to determine which processor will handle a prescription drug claim. Include PCN for the primary payer on the claim. If patient is uninsured or a cash payer, mark “CASH” in this field. If there is no PCN, mark “NONE” in this field.

_*Indicates a required field_  
​

### **Still have questions?**

If you have questions or need additional help, our team is here for you — please feel free to reach out using any of the contact options below:

* * *

Related Articles

*   [Rebate Model Frequently Asked Questions](https://support.beaconchannelmanagement.com/en/articles/9589827-rebate-model-frequently-asked-questions)
*   [Data Template: Medical Claims Data](https://support.beaconchannelmanagement.com/en/articles/9723390-data-template-medical-claims-data)
*   [Back-end Validations](https://support.beaconchannelmanagement.com/en/articles/13186516-back-end-validations)
*   [Medical Claims Validation Code Glossary](https://support.beaconchannelmanagement.com/en/articles/13186562-medical-claims-validation-code-glossary)
*   [Pharmacy Claims Validation Code Glossary](https://support.beaconchannelmanagement.com/en/articles/13186697-pharmacy-claims-validation-code-glossary)
```

---

## 2. How to read the excerpt

The page renders an HTML table with three columns, headed `Field`, `Data Type`,
`Description`. The proxy flattened each row onto one line, so a row reads:

    **<Field name>**<required asterisk, if any><Data Type> <Description>

The required marker is a literal `*` immediately after the bolded field name.
The page's own footnote defines it:

> `_*Indicates a required field_`

On this page **every one of the 11 fields carries the asterisk**. Beacon never
uses the words "required" or "optional" per field — the asterisk plus that
footnote is the entire mechanism.

---

## 3. Field table

Field order is exactly as published, top to bottom. Field names are spelled
exactly as Beacon spells them — capitalisation, spaces, and hyphens preserved,
not normalised.

| # | Field | Required (Beacon's marking) | Data Type (verbatim) | Description (verbatim) |
|---|-------|------------------------------|----------------------|------------------------|
| 1 | `340B ID` | `*` — required | `Alpha/Numeric` | The unique identification number provided by HRSA to the 340B covered entity . |
| 2 | `Date Prescribed` | `*` — required | `Standard date formats` | Date the prescriber wrote the prescription. |
| 3 | `Date of Service` | `*` — required | `Standard date formats` | Date on which the pharmacy filled the prescription. |
| 4 | `Rx Number` | `*` — required | `Numeric` | The native (unmodified) prescription number for the prescription as generated by the pharmacy. |
| 5 | `Fill Number` | `*` — required | `Numeric - 0-99` | Indicates the number of times a prescription has been filled. |
| 6 | `NDC-11` | `*` — required | `Numeric - 11 digits` | The 11-digit National Drug Code which indicates the manufacturer, product, and the commercial package size. |
| 7 | `Quantity Dispensed` | `*` — required | `Numeric` | The number of units dispensed to the patient. |
| 8 | `Prescriber ID` | `*` — required | `Numeric - 10 digits` | National provider identifier (NPI) of the physician that wrote the prescription. |
| 9 | `Service Provider ID` | `*` — required | `Numeric - 10 digits` | NPI of the pharmacy that filled the prescription. |
| 10 | `Rx Bin` | `*` — required | `Numeric - 6 digits` | Prescription Drug Bank Identification Number. Enables pharmacies to electronically transmit data to the appropriate PBM for processing and reimbursement. Include BIN for the primary payer on the claim. If patient is uninsured or a cash payer, mark “999999” in this field. |
| 11 | `Rx PCN` | `*` — required | `Alpha/Numeric` | Processor Control Number. Identifier used to determine which processor will handle a prescription drug claim. Include PCN for the primary payer on the claim. If patient is uninsured or a cash payer, mark “CASH” in this field. If there is no PCN, mark “NONE” in this field. |

**Count: 11 fields, 11 of them marked required, 0 not marked required.**

Transcription notes, so nothing above looks like a silent edit:

- Row 1's description ends `covered entity _._` in the raw text. The `_._` is a
  markdown italic wrapper the proxy emitted around the sentence's final period.
  The table renders it as a plain `.` with the stray space preserved before it.
- `Rx Bin` is spelled with a lowercase `in` — **`Rx Bin`**, not `Rx BIN` — even
  though its own description then writes "Include BIN for the primary payer".
  That is Beacon's inconsistency, reproduced rather than corrected.
- `NDC-11` is hyphenated. `340B ID` has a space. `Rx PCN` is upper-case.
- The data-type separator on this page is a plain hyphen (`Numeric - 11 digits`).
  The medical page uses an en dash (`Numeric – 11 digits`) for the same idea.
  Both are preserved as published; do not assume they are the same string.

---

## 4. Stated formats, lengths, allowed values

Everything the page states, and nothing more:

| Field | What Beacon states about format/length/values |
|-------|-----------------------------------------------|
| `340B ID` | Alpha/Numeric. No length, no pattern, no example given. |
| `Date Prescribed` | "Standard date formats". The page does not enumerate which formats. |
| `Date of Service` | "Standard date formats". Same — not enumerated. |
| `Rx Number` | Numeric. Must be "native (unmodified)". No length given. |
| `Fill Number` | Numeric, range `0-99`. |
| `NDC-11` | Numeric, exactly 11 digits. |
| `Quantity Dispensed` | Numeric. No precision or decimal rule given. |
| `Prescriber ID` | Numeric, exactly 10 digits (an NPI). |
| `Service Provider ID` | Numeric, exactly 10 digits (an NPI). |
| `Rx Bin` | Numeric, exactly 6 digits. Sentinel value: `999999` for uninsured/cash payer. |
| `Rx PCN` | Alpha/Numeric. Sentinels: `CASH` for uninsured/cash payer, `NONE` when there is no PCN. |

Three literal sentinel values appear on this page: **`999999`**, **`CASH`**,
**`NONE`**. They are the only concrete values Beacon publishes here.

---

## 5. What this page did not tell us

Recorded as gaps rather than filled in. Nothing below was inferred or completed.

1. **The downloadable template file was not obtained.** The page has a heading
   `## Download the pharmacy claims data template here:` followed by nothing in
   the extracted text. The actual attachment — almost certainly an Intercom-hosted
   file — did not survive text extraction. So the byte-exact header row of the
   real template (its column spelling, order, and whether it even matches this
   field list) is **unverified**. The field names above are the names as printed
   in the article's field-list table, which is not the same artefact as the file.
2. **No file format, delimiter, encoding, or header-row prose.** The page says
   nothing about CSV vs XLSX, comma vs pipe, UTF-8, quoting, or whether a header
   row is expected. None of that is on this page.
3. **No submission mechanism.** Nothing about SFTP, API, portal upload, filename
   convention, or cadence. Beacon documents that elsewhere — in
   `How to Submit Pharmacy Claims to Beacon` (article 9670187) and
   `How to Create a Column Mapping Template for Data Submissions` (article
   9707706). Neither was retrieved for this file.
4. **No example row and no example values** for any field, beyond the three
   sentinels in section 4.
5. **No validation codes or error behaviour.** Those live in
   `Pharmacy Claims Validation Code Glossary` (article 13186697), not retrieved here.
6. **"Standard date formats" is never defined.** Which formats Beacon accepts is
   genuinely not stated on this page. Do not assume ISO-8601.
7. **The "Still have questions?" contact block rendered empty.** The contact
   options Beacon refers to did not come through the proxy.
8. **No cardinality or uniqueness rules** — e.g. whether `Rx Number` + `Fill
   Number` is the intended natural key — are stated anywhere on this page.

---

## 6. Provenance

| Step | URL | Status |
|------|-----|--------|
| Direct fetch (control) | `https://support.beaconchannelmanagement.com/en/articles/9723402-data-template-pharmacy-claims-data` | **HTTP 403** |
| Proxy fetch (used) | `https://r.jina.ai/https://support.beaconchannelmanagement.com/en/articles/9723402-data-template-pharmacy-claims-data` | **HTTP 200**, 3336 bytes |
| Collection confirming the article | `https://r.jina.ai/https://support.beaconchannelmanagement.com/en/collections/9974649-data-templates` | **HTTP 200** |

Cross-check: the `Data Templates` collection (9974649) lists exactly 2 articles —
this one and the medical one — and its listing text matches this article's title
and subtitle, which corroborates that the proxy returned the right page.

Verification performed after writing: every field name in section 3 was checked
back against the verbatim excerpt in section 1. All 11 appear there. No field in
the table is absent from the excerpt.
